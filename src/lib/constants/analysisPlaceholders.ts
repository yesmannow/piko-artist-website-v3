/**
 * Placeholder values written by seedLibrary() for tracks that haven't been
 * analysed yet.  Both the seeding logic and the deferred-analysis trigger
 * (`needsAnalysis`) must use these constants so they stay in sync.
 */
export const PLACEHOLDER_BPM = '124';
export const PLACEHOLDER_KEY = '8A';
